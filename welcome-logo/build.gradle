plugins {
    id 'java-library'
}

group 'io.easeci.core'
version '0.0.1'

sourceCompatibility = '17'

repositories {
    mavenCentral()
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'domain')
    implementation project(':domain')
    testImplementation group: 'junit', name: 'junit', version: '4.12'
}

jar {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    def mainClassName = 'io.easeci.EaseciCoreApplication'
    manifest {
        attributes(
                'Class-Path': configurations.runtimeClasspath.collect { it.getName() }.join(' '),
                'Main-Class': "${mainClassName}",
                'Implements': 'io.easeci.extension.bootstrap.OnStartup',
                'Entry-Class': 'com.meksula.WelcomeLogo'
        )
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

afterEvaluate {
    tasks.getByName("jar") {
        dependsOn(":domain:build")
        dependsOn(":io-lib:build")
    }
}