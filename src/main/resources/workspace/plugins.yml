plugins:
  local:
    localisations:
      - <current>/plugins
      - <workspace>/plugins
    download: true
    threadpool:
      max-size: 100
  registry:
    timeout: 1000
    url: http://localhost:8080
  details:
    fetch-documentation: true

extensions:
  - plugin:
      name: bash
      version: 0.0.3
